#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

int main(){
     long double num;
     char numero[120];

     scanf("%LE", &num);

     sprintf(s,"%LE", X);

     if(numero[0] != '-') printf("+");

     printf("%.4LE\n", X);
   
     return 0;
}